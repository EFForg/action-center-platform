<%= form_for([:admin, @partner]) do |f| %>
  <% if @partner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partner.errors.count, "error") %> prohibited this partner from being saved:</h2>

      <ul>
      <% @partner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <%= f.label :name, "Organization Name" -%>
    <%= f.text_field :name, class: "form-control"  -%>
  </div>
  <div class="form-group">
    <%= f.label :privacy_url, "Privacy URL" -%>
    <%= f.text_field :privacy_url, class: "form-control" -%>
  </div>
  <div class="form-group">
    <%= f.label :code, "Abbrevation  (e.g. eff)" -%>
    <%= f.text_field :code, class: "form-control"  -%>
  </div>

  <div class="actions">
    <%= f.submit "Submit", class: "btn btn-success"   %>
  </div>
<% end %>
