<% content_for(:nav) { render "admin/nav" } %>
<div class="gallery">
  <div class="images">
    <%= render partial: "upload_and_search" %>

    <ul class="source-file-collection">
      <%= render partial: "source_file", collection: @source_files, as: :source_file,
        locals: { enable_delete: true } unless @source_files.empty? %>
    </ul>
  </div>
</div>
