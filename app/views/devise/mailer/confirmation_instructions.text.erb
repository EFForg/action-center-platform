Please confirm your new account using the link below:

<%= confirmation_url(@resource, :confirmation_token => @token) %>

If you didn't sign up for an <%= t(:site_title) -%> account, please ignore this message.

<%= root_url -%>
